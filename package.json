{
  "name": "pm-linear",
  "version": "2026.5.29",
  "description": "Linear.app issue sync for pm-cli",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/unbraind/pm-linear.git"
  },
  "bugs": {
    "url": "https://github.com/unbraind/pm-linear/issues"
  },
  "homepage": "https://github.com/unbraind/pm-linear#readme",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/",
    "manifest.json",
    "README.md",
    "CHANGELOG.md"
  ],
  "scripts": {
    "build": "tsc",
    "build:test": "tsc -p tsconfig.test.json",
    "test": "npm run build && npm run build:test && node --test dist-test/*.js",
    "dev": "tsc --watch",
    "prepack": "npm run build",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck",
    "audit:prod": "npm audit --omit=dev",
    "pack:dry-run": "npm pack --dry-run",
    "changelog": "pm-changelog --pm-root .agents/pm --mode prepend --output CHANGELOG.md --release-version-from-package --since-previous-tag --until-release-tag --item-url-base https://github.com/unbraind/pm-linear/blob/main/.agents/pm",
    "changelog:full": "pm-changelog --pm-root .agents/pm --mode replace --output CHANGELOG.md --all-release-tags --release-version-from-package --item-url-base https://github.com/unbraind/pm-linear/blob/main/.agents/pm",
    "changelog:check": "pm-changelog --pm-root .agents/pm --mode replace --output CHANGELOG.md --all-release-tags --release-version-from-package --item-url-base https://github.com/unbraind/pm-linear/blob/main/.agents/pm --check",
    "release:check": "npm run typecheck && npm run build && npm run audit:prod && npm run pack:dry-run && npm run changelog:check"
  },
  "peerDependencies": {
    "@unbrained/pm-cli": ">=2026.5.29"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "@unbrained/pm-cli": "^2026.5.29",
    "pm-changelog": "^2026.5.29-2",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "keywords": [
    "extension",
    "linear",
    "pm-cli",
    "pm-extension",
    "pm-package",
    "sync"
  ],
  "license": "MIT",
  "author": "@unbraind",
  "pm": {
    "aliases": [
      "linear"
    ],
    "extensions": [
      "."
    ],
    "catalog": {
      "display_name": "Linear Sync",
      "category": "sync",
      "summary": "Linear.app issue sync for pm-cli",
      "tags": [
        "linear",
        "sync",
        "import"
      ],
      "links": {
        "docs": "https://github.com/unbraind/pm-linear#readme",
        "npm": "https://www.npmjs.com/package/pm-linear",
        "repository": "https://github.com/unbraind/pm-linear",
        "report": "https://github.com/unbraind/pm-linear/issues"
      }
    },
    "docs": [
      "README.md",
      "CHANGELOG.md"
    ],
    "examples": [
      "README.md"
    ]
  }
}
